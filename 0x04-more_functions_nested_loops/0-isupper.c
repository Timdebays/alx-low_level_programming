#include "main.h"

/**
 * _isupper - Check if letter is uppercase
 * @x: The number to be checked
 * Return 1 if True and 0 if false
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
